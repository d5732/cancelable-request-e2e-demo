{
  "name": "cancelable-request-e2e-demo",
  "version": "0.0.0",
  "description": "propagate request termination from frontend to backend to database",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "start:backend": "cd backend && npm run start:dev",
    "start:frontend": "cd frontend && npm start",
    "start": "concurrently -k -n \"BACKEND,FRONTEND\" -c \"green,blue\" --handle-input \"npm run start:backend\" \"npm run start:frontend\""
  },
  "author": "d5732@github.com",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
